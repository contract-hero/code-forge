{
  "description": "Option D — one hook rule survives: test files are read-only during a cycle's green phase. Every other former forge-guard rule was either subsumed by the cycle child's /goal condition or dropped with Phase F / contract.md / single-turn fan-out enforcement.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/forge-guard.mjs\" pre-tool-use",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
